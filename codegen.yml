overwrite: true
schema: "typeDefs.graphql"
documents: null
generates:
  types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
